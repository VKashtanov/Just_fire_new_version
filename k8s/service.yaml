apiVersion: v1
kind: Service
metadata:
  name: just-fire-service-service
spec:
  selector:
    app: just-fire-service
  ports:
    - port: 8081
      targetPort: 8081
  type: LoadBalancer   #  it balances traffic between nodes
